from keys.mouse import MOUSE

from keys.default import KEYS as default_keys
from keys.hardware import KEYS as hardware_keys
from keys.apps import KEYS as app_keys

KEYS = default_keys + hardware_keys + app_keys

